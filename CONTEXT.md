# 小白Amax — 领域词汇表 (Ubiquitous Language)

> 本文件是领域的**术语定义**，不含实现细节。当前产品方向已收敛为：动作展示库 + 健身训练记录。

---

## 核心实体

### 用户 (User)
面向中国高校学生和健身新手的产品使用者。首版无需注册登录，所有基础功能在本设备本地可用。

### 动作 (Exercise)
动作库中定义的一个标准化健身动作。每个动作拥有唯一 ID、英文名称、目标肌群、协同肌群、所需器械、缩略图、GIF 动画和分步骤指令。当前动作库共有 1,324 个动作。动作是**只读定义数据**，用户不可修改。

### 动作中文别名 (ExerciseAlias)
应用侧维护的中文名称和常见搜索词，用于把用户熟悉的中文表达映射到原始动作数据。例如“卧推”“杠铃卧推”“bench press”应能命中对应动作。

### 训练记录 (Workout)
用户完成的一次训练会话。包含日期、所训练的动作列表及每组的详细数据。一次训练记录可包含多个动作。

### 训练动作 (WorkoutExercise)
训练记录中引用的某个动作。一个训练记录可以包含多个训练动作；每个训练动作下有多组训练组。

### 训练组 (TrainingSet)
训练记录的最小粒度。包含重量（kg）、次数（reps）和可选备注。一个训练动作可以有多个训练组，如卧推 3 组。

### 收藏动作 (FavoriteExercise)
用户在本设备收藏的常用动作，用于训练时快速添加。

### 最近动作 (RecentExercise)
用户最近添加到训练记录的动作，用于减少重复搜索。

### 本地数据库 (LocalDatabase)
浏览器 IndexedDB 中的应用数据，保存训练记录、训练组、收藏动作、最近动作和本地设置。

---

## 值对象 (非实体，但频繁使用)

- **目标肌群 (Target Muscle)**：动作的主要刺激肌肉，如"胸大肌""股四头肌"
- **器械 (Equipment)**：动作所需器材，如"杠铃""哑铃""自重"
- **训练部位 (Category/Body Part)**：动作对应的身体区域，如"胸部""背部""腿部"
- **RM (Repetition Maximum)**：在标准动作下能完成指定次数的最大重量
- **训练量 (Volume)**：单组或单次训练中的重量 × 次数汇总，用于粗略衡量训练工作量

---

## 核心关系

```
User ──1:N──> Workout ──1:N──> WorkoutExercise ──1:N──> TrainingSet
                                      └──> 引用 Exercise

Exercise (1,324条) <── 前端静态加载 ──> 动作搜索/浏览
User ──1:N──> FavoriteExercise ──> 引用 Exercise
```

---

## 边界上下文 (Bounded Contexts)

| 上下文 | 职责 | 核心实体 |
|--------|------|----------|
| **动作检索** | 搜索、筛选、浏览动作库，查看 GIF 与中英文指令 | Exercise, ExerciseAlias |
| **训练记录** | 记录每次训练的动作、组数、次数、重量数据 | Workout, WorkoutExercise, TrainingSet |
| **训练历史** | 回看历史训练并做基础统计 | Workout, TrainingSet |
| **本地偏好** | 收藏动作、最近动作、数据导入导出、本地设置 | FavoriteExercise, RecentExercise, LocalDatabase |
